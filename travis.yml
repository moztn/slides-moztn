language: python
python:
    - "2.7"
# Command to install dependencies
install:
    - pip install -r requirements.txt


notifications:
  email: false
  irc:
    channels:
      - "chat.freenode.net#mozillatunisia"
    on_success: change
    on_failure: always
    use_notice: true
    skip_join: true
